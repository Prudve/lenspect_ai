# Services Directory

Abstraction layer for data fetching:
- Initially serves data from `src/data/mockData.js`.
- In Step 10, will be seamlessly switched to Firebase Firestore / Cloud Functions without rewriting UI components.
